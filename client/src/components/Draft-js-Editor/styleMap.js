// use this style map to overwrite inline styles within the editor

const styleMap = {
  CODE: {
    backgroundColor: 'blue',
    fontFamily: '"Inconsolata", "Menlo", "Consolas", monospace',
    fontSize: 32,
    padding: 2,
  },
};

export default styleMap