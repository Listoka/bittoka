const postData = require('./mock-post-data.json')


// some number of times do
// get a post data obj { headings, paragraphs }
// make a post blocks array []
// wrap the paragraphs in <p> tags
// wrap the headings in <h2> tags, if they exist
// insert the headings and paragraphs into the blocks array
//    such that the headings are separated by paragraphs,
//    but otherwise it doesn't matter
// break the paragraphs up by words
// randomly pick some ranges to become inline code
// randomly pick some ranges to become bold, italic, etc
// randomly pick some ranges to become links
// re-stringify the paragraphs
// randomly pick a paragraph to be a blockquote sometimes
//    <pre><code></code></pre>
// randomly pick a paragraph to be a code block sometimes
//    <blockquote>



function createPostHtml() {

}